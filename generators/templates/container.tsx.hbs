import React from 'react';
import { useDispatch, useSelector } from 'react-redux';
import { select{{properCase name}}Store } from './reducer';
import Wrapper from './styles';

function {{properCase name}} (props) {
  const reduxState = useSelector(select{{properCase name}}Store);
  const dispatch = useDispatch();

  return (
    <Wrapper>
      {{properCase name}} is generated.
    </Wrapper>
  );
}

export default React.memo({{properCase name}});